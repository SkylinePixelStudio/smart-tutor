services:
  - type: web
    name: openvoice-api
    runtime: python
    buildCommand: pip install -r requirements.txt && python -m unidic download
    startCommand: python openvoice_server.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
